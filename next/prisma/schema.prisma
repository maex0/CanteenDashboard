generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model CatImage {
  id Int @id
  url String
  width Int
  height Int
  mime_type String
}
